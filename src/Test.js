import * as ShowLocation from './ShowLocation';
import AreaArray from './AreaArray.js';

var locationarray = AreaArray.data;

export function route_1() {
    ShowLocation.route_1.forEach.call(document.querySelectorAll(ShowLocation.route_1), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[0] + '</span>';
        
    });
}

export function route_2() {
    ShowLocation.route_2.forEach.call(document.querySelectorAll(ShowLocation.route_2), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[1] + '</span>';
        
    });
}

export function route_3() {
    ShowLocation.route_3.forEach.call(document.querySelectorAll(ShowLocation.route_3), function(route) { 
        route.classList.add("indent_show");    
        route.innerHTML = '<span class="route-tooltip">' + locationarray[2] + '</span>';         
    });
}

export function route_4() {
    ShowLocation.route_4.forEach.call(document.querySelectorAll(ShowLocation.route_4), function(route) { 
        route.classList.add("indent_show"); 
        route.innerHTML = '<span class="route-tooltip">' + locationarray[3] + '</span>';            
    });
}

export function route_5() {
    ShowLocation.route_5.forEach.call(document.querySelectorAll(ShowLocation.route_5), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[4] + '</span>';
    });
}

export function route_6() {
    ShowLocation.route_6.forEach.call(document.querySelectorAll(ShowLocation.route_6), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[5] + '</span>';
    });
}

export function route_7() {
    ShowLocation.route_7.forEach.call(document.querySelectorAll(ShowLocation.route_7), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[6] + '</span>';        
    });
}

export function route_8() {
    ShowLocation.route_8.forEach.call(document.querySelectorAll(ShowLocation.route_8), function(route) { 
        route.classList.add("indent_show");    
        route.innerHTML = '<span class="route-tooltip">' + locationarray[7] + '</span>';         
    });
}

export function route_9() {
    ShowLocation.route_9.forEach.call(document.querySelectorAll(ShowLocation.route_9), function(route) { 
        route.classList.add("indent_show");   
        route.innerHTML = '<span class="route-tooltip">' + locationarray[8] + '</span>';          
    });
}

export function route_10() {
    ShowLocation.route_10.forEach.call(document.querySelectorAll(ShowLocation.route_10), function(route) { 
        route.classList.add("indent_show");  
        route.innerHTML = '<span class="route-tooltip">' + locationarray[9] + '</span>';           
    });
}

export function route_11() {
    ShowLocation.route_11.forEach.call(document.querySelectorAll(ShowLocation.route_11), function(route) { 
        route.classList.add("indent_show");    
        route.innerHTML = '<span class="route-tooltip">' + locationarray[10] + '</span>';         
    });
}

export function route_12() {
    ShowLocation.route_12.forEach.call(document.querySelectorAll(ShowLocation.route_12), function(route) { 
        route.classList.add("indent_show");   
        route.innerHTML = '<span class="route-tooltip">' + locationarray[11] + '</span>';          
    });
}

export function route_13() {
    ShowLocation.route_14.forEach.call(document.querySelectorAll(ShowLocation.route_14), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[12] + '</span>';        
    });
}

export function route_14() {
    ShowLocation.route_14.forEach.call(document.querySelectorAll(ShowLocation.route_14), function(route) { 
        route.classList.add("indent_show");  
        route.innerHTML = '<span class="route-tooltip">' + locationarray[13] + '</span>';           
    });
}

export function route_15() {
    ShowLocation.route_15.forEach.call(document.querySelectorAll(ShowLocation.route_15), function(route) { 
        route.classList.add("indent_show");  
        route.innerHTML = '<span class="route-tooltip">' + locationarray[14] + '</span>';           
    });
}

export function route_16() {
    ShowLocation.route_16.forEach.call(document.querySelectorAll(ShowLocation.route_16), function(route) { 
        route.classList.add("indent_show");    
        route.innerHTML = '<span class="route-tooltip">' + locationarray[15] + '</span>';         
    });
}

export function route_17() {
    ShowLocation.route_17.forEach.call(document.querySelectorAll(ShowLocation.route_17), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[16] + '</span>';        
    });
}

export function route_18() {
    ShowLocation.route_18.forEach.call(document.querySelectorAll(ShowLocation.route_18), function(route) { 
        route.classList.add("indent_show"); 
        route.innerHTML = '<span class="route-tooltip">' + locationarray[17] + '</span>';            
    });
}

export function route_19() {
    ShowLocation.route_19.forEach.call(document.querySelectorAll(ShowLocation.route_19), function(route) { 
        route.classList.add("indent_show");   
        route.innerHTML = '<span class="route-tooltip">' + locationarray[18] + '</span>';          
    });
}

export function route_20() {
    ShowLocation.route_20.forEach.call(document.querySelectorAll(ShowLocation.route_20), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[19] + '</span>';        
    });
}

export function route_21() {
    ShowLocation.route_21.forEach.call(document.querySelectorAll(ShowLocation.route_21), function(route) { 
        route.classList.add("indent_show");   
        route.innerHTML = '<span class="route-tooltip">' + locationarray[20] + '</span>';          
    });
}

export function route_22() {
    ShowLocation.route_22.forEach.call(document.querySelectorAll(ShowLocation.route_22), function(route) { 
        route.classList.add("indent_show");  
        route.innerHTML = '<span class="route-tooltip">' + locationarray[21] + '</span>';           
    });
}

export function route_23() {
    ShowLocation.route_23.forEach.call(document.querySelectorAll(ShowLocation.route_23), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[22] + '</span>';        
    });
}

export function route_24() {
    ShowLocation.route_24.forEach.call(document.querySelectorAll(ShowLocation.route_24), function(route) { 
        route.classList.add("indent_show");
        route.innerHTML = '<span class="route-tooltip">' + locationarray[23] + '</span>';
    });
}

export function route_25() {
    ShowLocation.route_25.forEach.call(
        document.querySelectorAll(ShowLocation.route_25), function(route) { 
            route.classList.add("indent_show");
            // console.log("array proptype route 25" + ShowLocation.viridian_forest);
            // console.log("array proptype test");
        route.innerHTML = '<span class="route-tooltip">' + locationarray[24] + '</span>';
            
        }
    );
}

export function diglet_cave() {
    ShowLocation.diglet_cave.forEach.call(document.querySelectorAll(ShowLocation.diglet_cave), function(route) { 
        route.classList.add("indent_show");     
        route.innerHTML = '<span class="route-tooltip">' + locationarray[25] + '</span>';        
    });
}

export function viridian_forest() {
    ShowLocation.viridian_forest.forEach.call(
        document.querySelectorAll(ShowLocation.viridian_forest), function(route) { 
            route.classList.add("indent_show");
            route.innerHTML = '<span class="route-tooltip">' + locationarray[26] + '</span>';
            
        }
    );
}

export function mt_moon() {
    ShowLocation.mt_moon.forEach.call(document.querySelectorAll(ShowLocation.mt_moon), function(route) { 
        route.classList.add("indent_show");    
        route.innerHTML = '<span class="route-tooltip">' + locationarray[27] + '</span>';         
    });
}

export function rock_tunnel() {
    ShowLocation.rock_tunnel.forEach.call(document.querySelectorAll(ShowLocation.rock_tunnel), function(route) { 
        route.classList.add("indent_show");   
        route.innerHTML = '<span class="route-tooltip">' + locationarray[28] + '</span>';          
    });
}

export function power_plant() {
    ShowLocation.power_plant.forEach.call(document.querySelectorAll(ShowLocation.power_plant), function(route) { 
        route.classList.add("indent_show");    
        route.innerHTML = '<span class="route-tooltip">' + locationarray[29] + '</span>';         
    });
}

export function safari_zone() {
    ShowLocation.safari_zone.forEach.call(document.querySelectorAll(ShowLocation.safari_zone), function(route) { 
        route.classList.add("indent_show");  
        route.innerHTML = '<span class="route-tooltip">' + locationarray[30] + '</span>';           
    });
}

export function seafoam_islands() {
    ShowLocation.seafoam_islands.forEach.call(document.querySelectorAll(ShowLocation.seafoam_islands), function(route) { 
        route.classList.add("indent_show");  
        route.innerHTML = '<span class="route-tooltip">' + locationarray[31] + '</span>';           
    });
}

export function victory_road() {
    ShowLocation.victory_road.forEach.call(document.querySelectorAll(ShowLocation.victory_road), function(route) { 
        route.classList.add("indent_show");   
        route.innerHTML = '<span class="route-tooltip">' + locationarray[32] + '</span>';          
    });
}

export function indigo_plateu() {
    ShowLocation.indigo_plateu.forEach.call(document.querySelectorAll(ShowLocation.indigo_plateu), function(route) { 
        route.classList.add("indent_show");  
        route.innerHTML = '<span class="route-tooltip">' + locationarray[33] + '</span>';           
    });
}
